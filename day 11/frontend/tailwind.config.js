/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./public/**/*.{html,js}"], // Scan all HTML and JS files in public
    theme: {
        extend: {},
    },
    plugins: [],
}
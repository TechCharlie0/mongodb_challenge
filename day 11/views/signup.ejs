<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Sign Up - HeartMatch</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gray-100 p-4">
    <div class="w-full max-w-md bg-white rounded-xl shadow-lg p-8 space-y-6">
      <header class="text-center">
        <h1 class="text-4xl font-extrabold text-purple-600 mb-2">HeartMatch</h1>
        <p class="text-gray-600">Create your account and find your match!</p>
      </header>

      <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500 text-center"><%= error %></p>
      <% } %>

      <form action="/signup" method="POST" class="flex flex-col space-y-4">
        <input
          type="email"
          name="email"
          placeholder="Email"
          required
          class="p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
        />
        <input
          type="password"
          name="password"
          placeholder="Password"
          required
          class="p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
        />

        <div>
          <label for="gender" class="block text-gray-700 text-sm font-bold mb-1"
            >I am a:</label
          >
          <select
            name="gender"
            id="gender"
            required
            class="p-3 border border-gray-300 rounded-lg w-full focus:outline-none focus:ring-2 focus:ring-purple-500"
          >
            <option value="">Select Gender</option>
            <option value="male">Male</option>
            <option value="female">Female</option>
            <option value="other">Other</option>
          </select>
        </div>

        <input
          type="text"
          name="location"
          placeholder="Your Location (e.g., Kathmandu, Nepal)"
          required
          class="p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500"
        />

        <button
          type="submit"
          class="bg-purple-500 text-white p-3 rounded-lg font-semibold hover:bg-purple-600 transition duration-300 ease-in-out shadow-md"
        >
          Create Account
        </button>
      </form>

      <div class="relative flex items-center justify-center">
        <div class="flex-grow border-t border-gray-300"></div>
        <span class="flex-shrink mx-4 text-gray-400">OR</span>
        <div class="flex-grow border-t border-gray-300"></div>
      </div>

      <div class="flex flex-col space-y-3">
        <button
          onclick="location.href='/auth/google'"
          class="flex items-center justify-center bg-red-500 text-white p-3 rounded-lg font-semibold hover:bg-red-600 transition duration-300 ease-in-out shadow-md"
        >
          <img
            src="https://img.icons8.com/color/24/000000/google-logo.png"
            alt="Google"
            class="mr-2"
          />
          Sign Up with Google
        </button>
        <button
          onclick="location.href='/auth/facebook'"
          class="flex items-center justify-center bg-blue-600 text-white p-3 rounded-lg font-semibold hover:bg-blue-700 transition duration-300 ease-in-out shadow-md"
        >
          <img
            src="https://img.icons8.com/color/24/000000/facebook-new.png"
            alt="Facebook"
            class="mr-2"
          />
          Sign Up with Facebook
        </button>
      </div>

      <p class="text-center text-gray-500 text-sm">
        Already have an account?
        <a href="/login" class="text-pink-600 hover:underline">Login here</a>.
      </p>
    </div>
  </body>
</html>
